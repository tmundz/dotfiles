model = "gpt-5.5"
model_reasoning_effort = "high"

[tui]
status_line = ["model-with-reasoning", "current-dir", "five-hour-limit", "weekly-limit", "total-input-tokens", "total-output-tokens"]
status_line_use_colors = true

[features]
goals = true

# Sanitized for public dotfiles:
# - removed machine-specific trusted project paths
# - removed local model availability/onboarding state
# - never copy auth.json, history, logs, sqlite state, caches, or shell snapshots
